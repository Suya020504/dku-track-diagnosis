# Design System

<!-- impeccable:design-schema 1 -->

## Design Thesis

이 서비스는 행정 포털이나 SaaS 대시보드가 아니라, 신입생이 펼쳐 보는 `캠퍼스 안내책자 × 학업 플래너`다. 전공 과목에서 모듈·트랙·다음 학기로 이어지는 경로를 하나의 컴퍼스 라인으로 보여 준다. 기존 화면의 반복적인 흰 카드 그리드, 큰 수치 요약, 기본 브라우저 버튼을 시각 권위로 사용하지 않는다.

메인 경험은 `전공 여정 캠퍼스 지도`다. 학생은 관심 탐색 경로 또는 이수 과목 자가진단 지름길에서 출발해 현재 위치·부족 영역·다음 과목으로 이동한다. 학기 플래너는 지도 끝의 선택 목적지이며 진단 완료를 막지 않는다.

## Selected First-Surface Direction

### THESIS

관심 질문을 시작하면 전공 로드맵과 학기 플래너가 점진적으로 펼쳐진다. 대학 진단 서비스가 흔히 사용하는 정적 히어로와 통계 카드 대시보드를 거부한다.

### OWN-WORLD

왼쪽의 안내책자 색인, 중앙의 Compass Path Ribbon, 과정에 따라 펼쳐지는 학기 플래너, 하늘·민트 종이 면과 단국 블루·학과 녹색의 정밀한 구분을 전 화면의 공통 문법으로 사용한다.

### STORY

학생은 관심 질문으로 현재 위치를 찾고, 다섯 트랙을 비교한 뒤, 완료 과목과 부족 조건을 연결해 자신의 학기 계획을 저장한다. 이미 트랙을 아는 학생은 직접 진단으로 같은 경로에 합류한다.

### FIRST VIEWPORT

상단에서 `지도 안내`를 활성화하고, 화면 대부분을 평평한 전공 여정 개념 지도가 차지한다. 지도 위에는 `내 관심 트랙 찾기` 녹색 경로와 `이수 과목 바로 진단` 파란 지름길이 실제 버튼·주소로 놓이며, 두 경로는 현재 진단에서 직선으로 합류한다. 부족 영역과 다음 과목이 같은 수평축의 후속 도착지이고 학기 플래너는 `선택` 분기로 분리한다.

### FORM

사용자가 선택한 `C · 학업 플래너형`, staging `planner timeline first`, seed `user-pinned / no concept roll`을 적용한다. 생성 시안은 north star이며 실제 로고·사용자명·예시 수치·작은 문구·표를 그대로 복제하지 않는다.

## Surface Modes

- 랜딩: Persuade. 관심 트랙 찾기가 한눈에 보이는 주 행동이며, 이수 과목 바로 진단은 빠른 보조 경로다.
- 설문·프로필·과목 선택·계획 입력: Operate. 한 화면에 한 가지 결정과 하나의 주 행동을 둔다.
- 트랙 가이드: Read. 정의·장점·5개 구성·공식 영상을 각각 독립 주소에서 읽는다.
- 교육과정·공식 근거: Read. 색인과 출처를 따라 읽는 안내책자 구조를 사용한다.
- 결과·추천·학기 계획: Operate + Read. `현재 → 다음 → 확인` 순서로 판단과 행동을 연결한다.

### Purpose-specific spaces

- 지도 안내: 탐색·현재 위치 — 하늘색과 단국 블루, 조작 컨트롤 없는 고정형 평면 지도와 실제 목적지 버튼
- 트랙 가이드: 이해·근거 확인 — 녹색 읽기 면, 4개 주소형 색인, 사용자 동의 뒤 공식 영상 재생
- 관심 트랙: 발견·선택 — 학과 녹색과 밀색, 다섯 방향 비교
- 자가진단: 입력·확인 — 파스텔 민트 원장과 직접 선택
- 결과: 판단·다음 행동 — 블루·청록 경로와 근거 띠
- 학기 플래너: 선택형 계획 — 따뜻한 종이색·민트 학기 열
- 공식 자료: 근거 확인 — 절제된 네이비와 읽기 페이지

## Own World

### Material language

- 캠퍼스 오리엔테이션 안내책자, 학업 플래너, 평면 안내도, 색인 탭, 전공 스티커에서 문법을 가져온다.
- 장식용 카드 대신 페이지 띠, 수평 구역, 색인 탭, 경로선, 체크 마크가 구조를 설명한다.
- 모서리는 모든 곳에 동일하게 둥글리지 않는다. 큰 종이 면은 20px, 조작 컨트롤은 10–12px, 표·색인·과목 행은 4–8px을 사용한다.
- 유리 질감, 네온, 과도한 그라데이션, 떠 있는 아이콘 타일, 임의의 01/02/03 장식을 금지한다.

### Color strategy

Full palette. 밝은 자연광 아래에서 노트북으로 보는 학생을 기준으로 한다.

| 역할 | 토큰 | 색상 |
|---|---|---|
| 종이 바탕 | `--paper` | `#F8FBFC` |
| 하늘 안내 면 | `--sky-sheet` | `#DDF2FC` |
| 민트 진행 면 | `--mint-sheet` | `#DDF3E8` |
| 단국 정보색 | `--dankook-blue` | `#174B82` |
| 학과 행동색 | `--department-green` | `#087A58` |
| 본문 잉크 | `--ink` | `#14243A` |
| 보조 잉크 | `--ink-muted` | `#5C6C7E` |
| 농식품 강조 | `--wheat` | `#F2C66D` |
| 확인 필요 | `--review-coral` | `#E67C62` |

단국 블루는 정보·공식 근거, 학과 녹색은 주 행동·완료 상태, 밀색은 비교·가설, 코랄은 확인 필요에만 사용한다. 색만으로 상태를 전달하지 않는다.

### Typography direction

- Display token `--planner-font-display`: `Gmarket Sans`, `GmarketSans`, `Pretendard Variable`, `Pretendard`, `SUIT Variable`, `SUIT`, `Noto Sans KR`, `Malgun Gothic`, `sans-serif` 순서다.
- Body/UI token `--planner-font-body`: `Pretendard Variable`, `Pretendard`, `SUIT Variable`, `SUIT`, `Noto Sans KR`, `Apple SD Gothic Neo`, `Malgun Gothic`, `system-ui`, `sans-serif` 순서다.
- 두 스택은 사용자 기기에 이미 설치된 글꼴 후보와 시스템 fallback만 사용한다. 앱이 글꼴 파일을 번들하거나 원격 요청·설치를 수행한다고 주장하지 않는다.
- Display stack은 페이지 H1과 랜딩·설문의 주요 간행물형 제목에만 적용하고, 버튼·입력·유틸리티 조작은 Body/UI stack을 유지한다.
- Data/utility: 본문 글꼴의 tabular numerals와 굵기 차이를 사용한다. 별도 영문 모노 글꼴로 기술 제품처럼 보이게 하지 않는다.
- 본문은 16–18px, 라인 높이 1.65 전후. 모바일의 핵심 제목은 30–38px, 데스크톱 랜딩 제목은 64–80px 범위에서 내용에 맞춰 조정한다.

## Signature System

`Compass Path Ribbon`이 전 화면을 연결한다.

```text
관심 질문 ── 과목 ── 모듈 ── 트랙 ── 다음 학기
     현재 위치 ●              다음 행동 →
```

- 랜딩에서는 컴퍼스 일러스트 속 경로와 실제 CTA를 연결한다.
- 진단에서는 현재 단계와 되돌아갈 수 있는 이전 단계를 표시한다.
- 결과에서는 `현재 상태 → 다음 과목 → 계획 → 공식 확인`의 읽기 순서를 만든다.
- 장식용 선이 아니라 주소·단계·상태와 일치하는 실제 내비게이션이어야 한다.

## Imagery

- 메인 런타임 이미지는 텍스트·로고가 없는 `campus-compass-illustration.webp` 계열이다.
- 공식 캠퍼스 사진은 재사용 허락 전까지 앱 화면에 카드·미리보기·원문 링크로 노출하지 않고, 출처와 권한 검토 기록만 내부 문서에 보존한다.
- 생성 이미지는 `개념 설명 이미지`로 표시하고 공식 캠퍼스·교육과정 증거처럼 사용하지 않는다.
- UI 수치·진행률·과목표·공식 로고는 이미지에 넣지 않고 HTML/CSS/SVG와 기존 아이콘으로 구현한다.
- 트랙별 표식은 장바구니, 지도, 트럭, 차트, DNA의 일관된 선형 glyph를 사용한다.

## Layout Rules

- 최대 본문 폭은 1180–1240px. 넓은 화면에서도 읽기 열을 지나치게 벌리지 않는다.
- 랜딩 첫 화면은 텍스트 약 45%, 컴퍼스 장면 약 55%로 구성하되 일반적인 직사각형 이미지 카드처럼 가두지 않는다.
- 한 화면에 핵심 질문 하나, 주 행동 하나. 보조 행동은 텍스트 링크나 낮은 강조 버튼이다.
- 결과는 `현재`, `다음`, `확인`의 세 구역을 한 화면에 겹치지 않고 주소·탭·접힘으로 분산한다.
- 표는 비교가 필요한 데이터에만 사용하고 모바일에서는 의미 단위 행 또는 세로 planner로 전환한다.
- 흰 카드 연속 배치를 피하고, 큰 배경 면·띠·구분선·색인 탭으로 계층을 만든다.

## Component Character

- Primary action: 녹색의 단단한 직사각형, 48–54px 높이, 아이콘은 행동 방향을 보조한다.
- Secondary action: 종이 면 위 선형 버튼 또는 밑줄 링크. Primary와 같은 무게를 갖지 않는다.
- Page navigation: 결과·추천·공식 자료는 탭 위젯을 흉내 내지 않고 주소가 바뀌는 색인 버튼과 `aria-current="page"`를 사용한다.
- Course row: 체크박스, 과목명, 모듈 glyph, 학점·학기를 한 줄의 planner 행으로 표시한다.
- Result summary: 큰 숫자 네 칸 대신 상태 문장, 근거 배지, 다음 행동을 한 흐름으로 묶는다.
- Evidence: 각주·출처 띠·확인 질문으로 표시하고 공식 여부를 텍스트로 명시한다.
- Empty/error: 무엇이 없고 다음에 무엇을 하면 되는지 한 문장과 한 행동으로 안내한다.

## Motion

- 랜딩 지도는 확대·축소·드래그 없이 고정하며, 모든 목적지를 한 화면에서 읽게 한다.
- 단계 이동은 160–240ms의 면 전환과 초점 이동으로 설명한다.
- hover 장식을 여러 곳에 흩뿌리지 않는다.
- `prefers-reduced-motion`에서는 모든 이동 애니메이션을 제거하고 상태를 즉시 표시한다.

## Responsive Behavior

- 900px 이하에서 랜딩은 텍스트 → 주 행동 → 컴퍼스 이미지 순서로 전환한다.
- 680px 이하에서는 색인 탭을 가로 스크롤시키지 않고 2열 또는 선택 메뉴로 바꾼다.
- 모바일 전공 여정 지도는 전체 목적지를 담은 고정형 아이콘 지도와 세로 경로 목록을 함께 제공한다. 잠긴 목적지는 이유를 함께 표시한다.
- 학기 계획은 데스크톱의 가로 학기 열을 모바일에서 세로 시간축으로 전환한다.
- 하단 고정 행동이 내용을 가리지 않게 safe-area와 실제 버튼 높이를 확보한다.
- 긴 과목 목록에는 `결과로 건너뛰기`와 학년·학기/모듈 그룹 내비게이션을 제공한다.

## Accessibility Floor

- 모든 핵심 조작은 44px 이상이며 키보드·스크린리더로 동일한 순서에 접근한다.
- focus-visible은 세계에 맞는 청록색 이중 윤곽으로 스타일링하고 브라우저 기본 검은 윤곽에 의존하지 않는다.
- 상태는 색·아이콘·텍스트를 함께 사용한다.
- 페이지/단계 전환 시 새 H1으로 초점을 이동하고 스크롤을 맨 위로 복원한다.
- 실제 버튼·링크·입력 의미를 유지하며 시각화를 클릭 불가능한 이미지로 대체하지 않는다.

## Print And History

- 결과와 학기 계획은 브라우저 인쇄에서 공통 shell·색인·하단 내비게이션·화면 전용 행동을 숨기고 활성 본문만 폭 제한 없이 출력한다.
- 추천 기준, 결과 페이지, 자료 페이지는 URL 상태를 사용하며 뒤로가기·앞으로가기·새로고침 후 같은 활성 페이지를 복원한다.
- legacy `?view=overview`는 새 `track-guide/overview`로, `?view=modules`는 `?view=resources&section=modules`로 주소를 교체한다. `lab`·`experiment` 별칭은 현재 추천·계획 경로로만 정규화한다.

## Implementation Status

- 구현됨: 공통 GuidebookShell, 고정형 지도 랜딩, 트랙 가이드 4페이지, 관심 설문, 2단계 프로필, 과목 원장, 결과 3페이지, 독립 추천 3축, 학기 플래너, 공식 자료 4페이지, 반응형·인쇄·키보드·주소 복원.
- 런타임 identity: 공식 logo·seal·학과 마크 이미지는 사용하지 않고 텍스트 워드마크와 Compass 표식만 사용한다. 공개 자산 파일은 삭제하지 않았다.
- 검증된 경계: 1440×900, 1024×768, 768×1024, 390×844, 320×800에서 가로 넘침·44px 미만 가시 조작·중복 main/H1이 없다. 최종 수정에서는 1440px 랜딩 H1 66.24px, 390/320px 어절 단위 줄바꿈, 세로 Compass Path와 모바일 하단 내비게이션 비겹침을 추가 확인했다.
- 남은 release gate: PDF beta의 실제 포털 성적표 표본 매칭과 공개 범위 확정, 공식 캠퍼스 사진의 외부 재사용 허락, blocked-by-public-access 상태인 개설 이력의 공식 재확인.

## Absolute Bans

- 모든 정보를 한 페이지나 하나의 대시보드에 몰아넣기
- 동일한 흰색 둥근 카드 반복
- 가짜 학교 로고, 생성 이미지 속 공식 로고·수치·긴 한글 표
- 공식 판정처럼 보이는 `졸업 가능`, `이수 확정` 문구
- 출처가 확인되지 않은 캠퍼스 사진의 전면 배포
- PDF 원문·파일명·개인정보를 UI 장식 또는 예시 데이터로 사용
