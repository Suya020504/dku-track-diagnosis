# 변경 기록

이 문서는 GitHub 커밋 목록만 봐서는 파악하기 어려운 변경 목적과 검증 결과를 협업자가 빠르게 이해할 수 있도록 정리합니다.

## 2026-09-09

### 학생용 자료 화면과 전공 유형별 안내 정리

- 교육과정 47과목을 학년·학기별 교과로드맵 표로, 시간표 37분반을 요일별 표와 분반 목록으로 구성했습니다. 검색·학년/요일 필터·과목 상세·빈 결과 초기화를 유지합니다.
- 학생용 화면에서 검증 기록, 파일 해시·크기, 자료 불일치 메모와 반복 면책을 제거했습니다. 근거 기록은 `docs/research/`에 보존하고 배포 업로드에서는 제외합니다. PDF·HWP 원문은 공개하지 않습니다.
- 주전공·복수전공·부전공 안내, 트랙명 표기 예시, 2026년 1학기 신청 마감·원본 제출·삭제 문의 경로를 반영했습니다. 과거 영상의 신청 절차와 현재 신청 안내를 구분합니다.
- 학과 입학생의 입학연도별 전공필수를 별도 체크리스트로 추가했습니다. 기존 모듈 내 필수 계산과 학생 저장 데이터는 바꾸지 않았으며, 충족 제목과 추천 문구에 계산 범위를 명시했습니다.
- 전체 802개 테스트, 타입 검사·빌드 통과. 모바일·태블릿·1080p PC의 표, 필터, 키보드 상세, 자료 이동과 저장된 결과 재진입을 확인했습니다. 자세한 범위는 [검수 기록](docs/research/2026-09-09-student-resources-qa.md)에 남깁니다.
- 기능 커밋 `9bb2a20`과 배포 `dpl_3RGGoJn2xnnnjNzDHshtvY4reqP7`을 운영에 반영했습니다. 승격 전·운영의 대표 조회 흐름을 각각 확인했고, 원문 PDF와 내부 감사 문서 경로는 운영에서 404입니다.

## 2026-09-08

### 학생용 가이드의 출처를 한곳으로 정리

- 가이드 개요·장점·학위·구성·신청 준비에 흩어진12개 근거 링크를 기존 ‘공식 영상·자료’ 화면으로 모았습니다. 각 읽기 페이지에는 공통 자료 보기 버튼 하나만 둡니다.
- 장점 화면의 반복되는 ‘공식 설명과 활용 방법’ 펼침4개를 없애고 학생이 바로 이해할 수 있는 설명으로 정리했습니다. 증명서06:50·중간 신청04:19 영상 구간 링크도 자료 화면에 보존했습니다.
- 상담 전화·위치 링크와 현재 학사 인정의 불확실성, PDF 원문 비공개 정책은 유지합니다. 진단·저장·계산 규칙은 바꾸지 않았습니다.
- 관련25개 테스트 및 전체746개 테스트·타입·빌드·diff 검사를 통과했습니다. 가이드6화면×5크기30건, 자료 이동·새로고침·뒤로가기25개 경로를 로컬·승격 전 배포·운영에서 각각 확인했습니다.
- 기능 커밋 `ca32574`와 배포 `dpl_39n3EnxviEBsPHpC9ki19RkSQwFm`을 운영에 반영했습니다. 기존 PDF 원문 URL은 운영에서 계속404이며, 학생 데이터와 학사 계산은 변경하지 않았습니다.

### 제공 PDF 대조와 실제 이수 지원 기능 · 운영 반영

- 복수전공·부전공 입력 화면의 구형 필수과목 배지를 실제 이수 경로의 계산 기준에 맞췄습니다. 최종 결과 엔진의42·21학점 기준은 유지합니다.
- 과목 입력을 검색·학년 필터·짧은 목록으로 바꾸고 완료/수강 중/계획 상태를 실제 저장합니다. 모바일 목록은6개씩 나눕니다.
- 트랙 밖 과목과 기타 인정학점을 완료 전공학점으로 별도 입력합니다. 중복 합산을 막고, 트랙 모듈에는 넣지 않습니다.
- 추천 과목의 계획 담기·해제·학기 변경을 실제 입력과 연결했습니다. 진단 결과만 보관하는 기능과 읽기 전용 저장 기록 화면도 추가했습니다.
- 개인 개발자 홍보 대신 학과 공식 연락·자료와 신청 전 확인할 내용을 안내합니다. 새 PDF의 내부 표기 차이를 결과에도 구체적으로 표시합니다.
- 자연스러운 학생용 문구를 보존하고, 저장소 접근 자체가 차단돼도 화면이 열리는 복구 경로와 모바일 조작 영역을 보강합니다.

제공 PDF 원문은 사용자 요청대로 비공개로 보존하고 GitHub·배포에서 제외합니다. 교육과정 계산·제공 출처·참고 쪽수와 별도의 학과 공개 자료 링크는 유지합니다. 전체 검증·GitHub 및 Vercel 반영 상태는 [서비스 마감 검수](reports/validation/2026-09-08-service-completion-qa.md)에서 확인합니다.

기능 커밋 `ed7a6b9`를 작업 브랜치에 푸시하고 배포 `dpl_Hip4cXPrZ4J19Maty7WLBq7vcVc2`를 운영 주소로 승격했습니다. 최종741개 테스트·타입·빌드 통과. 승격 전·후 핵심20개, 복구17개, 신규 방문자/시간표/설문 흐름과 비공개 안내6조합을 확인했습니다. 운영31화면×9크기279건도 통과했습니다. 원문 PDF 경로는 운영에서404이며 파일은 공개하지 않았습니다.

### 페이지별 재구성과 공식 시간표 추가

- 홈·가이드·입력·결과·추천·플래너·자료·문의 화면을 각 목적에 맞게 새로 구성했습니다. 공통 셸은 브랜드와 이동만 담당하고, 과거 페이지 스타일이 새 화면을 덮던 규칙은 분리했습니다.
- 필수 진단은 이수 유형→이수 과목→진단 결과의 세 단계입니다. 모듈 자료, 관심 설문과 학기 계획은 선택 기능으로 구분했습니다. 가이드의 전체 출처 목록은 공식 영상·자료 페이지에만 표시합니다.
- 공식 학과 교육과정 47과목과 2026-2 천안 실제 37분반을 추가했습니다. 학사 과목코드·분반·교수·요일·교시·시각·강의실·수업방식을 검색할 수 있습니다. 원문명 전사, 야간 교시, 토요일과 미표기를 확인했습니다.
- 과목 선택은 짧은 체크 행, 결과는 세부 학점과 남은 조건, 추천은 기준별 비교, 학기 계획은 실제 과목과 미정 학점 예약을 구분하는 구성으로 바꿨습니다.
- 새 트랙 후보와 확정 목표를 분리했습니다. 확인 전 이동·새로고침은 기존 결과와 계획을 유지하며, 확인 완료 후에만 새 목표와 참고용 조건을 적용합니다. 기존 v2 저장값과 호환됩니다.
- 모바일 메뉴의 키보드 열기·닫기·포커스 복원, 저장 오류 표시, 입학연도 수정, 숨겨진 필터 상태, 링크 이름과 대비를 보완했습니다.

제품 변경: `29fb43c..3e31b8b`. 전체 58파일·653테스트, 타입 검사·프로덕션 빌드, 23개 화면×9개 크기(207개 조합), 핵심 흐름과 오류 복구를 검증했습니다. 상세 근거와 배포 후 확인은 [검수 기록](reports/validation/2026-09-08-page-rebuild-qa.md)에 정리합니다. 공식 데이터의 범위·조회 조건은 [자료 반영 기록](docs/research/2026-09-08-official-curriculum-timetable.md)에 있습니다.

배포 소스 `c4494a8`을 Vercel 배포 `dpl_HbVcZpZ6CSh3mfVGsyzows5vWjKq`로 운영 주소에 반영했습니다. 공개 운영 사이트에서도 207개 반응형 조합, 진단·시간표·설문 핵심 흐름, 저장 복구 17개와 후보 확인 12개 검사를 통과했습니다. 보호된 배포 전용 주소는 인증 CLI로 검증했으며 보호 설정을 해제하지 않았습니다.

### 페이지 목적별 디자인과 설문 재방문 보완

- 홈은 크림색, 가이드는 민트, 진단은 하늘색, 결과는 네이비, 추천은 청록, 플래너는 밀색, 자료는 아이스블루로 구분했습니다. 공통 DKU 헤더와 주요 실행 버튼의 의미는 유지합니다.
- 구형 진단 셸의 회색 외곽을 공통 배경에 맞추고, 과목 검색·접이식 목록·선택 개수·결과 확인을 한 흐름으로 다듬었습니다.
- 모바일에서는 반복 단계 표시와 선택 요약의 높이를 줄였습니다. 설문의 다섯 응답은 한 줄에서 비교할 수 있고, 방향키 포커스는 바깥 테두리로 표시됩니다.
- 홈·추천 탭·비교 결과에서 관심 설문에 재진입해도 저장된 소속·답변·진행 위치·완료 결과를 복원합니다. 다른 소속으로 바꾸거나 `다시 답하기`를 누른 경우에만 설문을 새로 시작합니다.
- 학업 책상·과목/모듈 구조·학기 계획의 기존 WebP 3개(합계 125,832 bytes)를 재사용합니다. 이전 지도 이미지와 구형 화면은 현재 앱 진입 경로에서 사용하지 않습니다.

검증:

- 설문 재진입 실패 테스트 3개와 추천 탭 복원 실패를 먼저 재현한 뒤 수정했습니다.
- 관련 3파일·30테스트, 전체 53파일·598테스트, TypeScript, Vite 프로덕션 빌드, diff check 통과.
- Chromium 344×882, 360×800, 390×844, 430×932, 768×1024, 820×1180, 1024×768, 1920×1080에서 주요 10개 화면을 확인했습니다. 가로 넘침·깨진 이미지·main/h1 중복·프레임워크 오류가 없었고, 첫 화면의 작은 글씨 대비를 보완했습니다.
- 과목 체크 1→2개 선택과 새로고침 복원, 결과 뒤로/앞으로 이동, 학기 계획 저장·복원, 관심 설문 재진입·완료·재시작·소속별 질문 전환, 손상된 저장값과 저장 차단 복구를 확인했습니다.
- 구현 커밋 `dc93697`을 GitHub 작업 브랜치에 반영하고 Vercel 배포 `dpl_5tzC1ZafacXuR9hMqaZBfyXcyLGJ`를 운영 주소에 연결했습니다. 공개 URL에서 390px 과목 체크→새로고침→결과와 430/1920px 가이드·자료·추천·플래너 준비 화면을 재검수했습니다. 앱 콘솔 오류·경고는 없었습니다.
- 보호된 배포 전용 주소는 Vercel 인증 CLI로 HTML/번들을 확인했고, 실제 브라우저 검수는 운영 주소에서 수행했습니다. 보호 설정을 해제하지 않았습니다.

남은 경계: 실물 기기의 Safari·Samsung Internet 검증은 수행하지 않았습니다. PDF 자동 입력 beta와 개인별 학위·트랙 인정은 기존 확인 범위를 유지합니다. 과거 기록의 미배포 표시는 해당 날짜의 상태입니다.

## 2026-09-03

### Unify the service shell and clarify degree versus track outcomes

- 도구·정보와 문의 화면에 남아 있던 구형 좌측 `GUIDE INDEX` 셸을 제거하고, 모든 화면이 같은 상단 주요 서비스 내비게이션과 본문 시작점을 사용하도록 통합했습니다.
- 구형 `GuideIndex` 컴포넌트·테스트·전용 스타일을 삭제했습니다. 과거 공유 주소 `lab`, `experiment`, `overview`, `modules`는 별도 더미 화면을 열지 않고 현재 정규 주소로 복구됩니다.
- 트랙 가이드에 `학위·이수 결과`를 독립된 3번째 페이지로 추가해 정식 학위명, 전공명, 트랙 기록을 분리했습니다.
- 2026년 5월 7일 개정 학칙 별표 2에서 식품자원경제학과의 학위명 `경제학사`, 전공명 `식품자원경제학`을 재확인했습니다.
- 2024 학과 공식 영상에서 설명한 학위증·성적증명서의 트랙명 표기와 2~4학년 중간 신청 가능성을 시간 링크로 연결하되, 2026 실제 표기·신청 절차는 학과 확인 필요로 표시했습니다.
- 자료 페이지별 브라우저 제목을 `5개 트랙 자료`, `모듈·과목 자료`, `2026 교육과정 자료`, `공식 근거·문의`로 구체화했습니다.
- 과목 입력의 별도 결과 사이드카드를 제거하고 `이수 완료·수강 중·계획 → 진단 결과 확인`을 하나의 상단 행동 영역으로 통합했습니다.
- 45개 과목은 학기별 접이식 체크리스트로 바꾸고, 첫 그룹만 펼친 상태에서 과목명·학점·상태만 보여 줍니다. 코드·모듈·근거는 과목별 `과목 정보`에서 선택적으로 확인합니다.
- 모듈 바로가기 칩과 장식형 진행 단계를 제거하고, 검색·학기/모듈 보기·추가 필터와 60px 평면 진행 표시로 위계를 단순화했습니다.
- 랜딩·트랙 구조·학기 계획 이미지에서 나침반·경로 표현을 제거하고, 과목 체크 카드·모듈 폴더·학기 플래너 중심 이미지로 교체했습니다.
- 추천 결과의 `Compass Path`, 가이드의 지도 비유, 랜딩의 `현재 위치` 표현을 각각 `기준 비교 요약`, `연결 구조`, `현재 진행도`로 통일했습니다.
- 1078×912 기준 문서 높이는 6,975px에서 1,695px로, 과목 영역 높이는 5,793px에서 667px로 줄고 입력 폭은 638px에서 942px로 늘었습니다.

경계와 남은 리스크:

- 트랙별 별도의 학사학위가 수여된다는 최신 공식 근거는 확인되지 않았습니다. 현재 공식 학위명은 경제학사이며, 트랙명 병기는 2024 공식 영상의 운영 설명입니다.
- 운영 Vercel에는 아직 배포하지 않았습니다.

## 2026-09-02

### Rebalance the guide hierarchy and finish the interaction QA

- 트랙 가이드에서는 진단용 `학업 여정` 리본을 숨기고, GUIDE INDEX와 4개 가이드 탭만 남겨 첫 화면에서 가이드 제목과 목적이 바로 보이게 했습니다.
- GUIDE INDEX의 `트랙 가이드·탐색`을 `트랙 가이드`로 줄여 모바일과 좁은 데스크톱에서 줄바꿈 없이 읽히게 했습니다.
- 과목 선택 제목의 브라우저 기본 검은 포커스 테두리를 학과 초록 포커스 링으로 통일했습니다.
- 공식 자료 외부 링크의 실제 클릭 영역을 최소 44px로 늘려 모바일 접근성을 보완했습니다.
- 관심 설문, 직접 진단, 결과 비교, 선택형 학기 플래너를 실제 브라우저에서 완주하고 주소·저장·뒤로가기·복구 상태를 다시 확인했습니다.

검증:

- npm.cmd test -- --run — Vitest 4.1.8, 51개 테스트 파일·579개 테스트 통과
- npm.cmd run build — TypeScript noEmit과 Vite 7.3.5 프로덕션 빌드 통과, 1,778개 모듈 변환
- git diff --check — 공백 오류 0건
- Chromium — 390×844·430×932·1024×768·1440×900에서 주요 5개 화면 가로 overflow 0건, 모바일 고정 내비게이션 겹침 0건, 콘솔 오류와 개발 오류 오버레이 0건
- 키보드 — 랜딩 H1에서 Tab으로 주 행동에 이동하고 Enter로 관심 설문에 진입, 포커스 링과 화면별 제목·랜드마크를 확인

경계와 남은 리스크:

- YouTube 재생 시 Chromium의 Windows WebGPU `powerPreference` 경고 1건이 있었지만 앱 오류는 아니며 콘솔 오류는 0건입니다.
- 실제 학적·수강 데이터와 운영 Vercel 배포는 이번 검수 범위에 포함하지 않았습니다.

## 2026-08-31

### Add the official track guide and simplify the campus map

- 지도 확대·축소·현재 위치·드래그 기능을 제거하고, 7개 목적지를 한 번에 읽는 고정형 평면 지도로 단순화했습니다.
- 랜딩 상단에 `관심 또는 바로 진단 → 현재 위치와 부족 영역 → 다음 과목 또는 선택형 플래너`의 3단계 요약을 추가했습니다.
- `?view=track-guide&section=overview|benefits|structure|videos` 주소를 가진 별도 트랙 가이드를 만들고, 각 페이지에 하나의 목적만 남겼습니다.
- 구형 `?view=overview`는 새 가이드 첫 페이지로 정규화하고 중복 안내 메뉴를 제거했습니다.
- 학교 학칙, 2026 학사종합안내, 학과장 인사말, 학과 교육과정, 학과 사무실과 공식 YouTube를 원문 링크로 연결했습니다.
- 학과 공식 4부작 영상을 사용자가 재생을 누른 뒤에만 `youtube-nocookie.com` 플레이어로 불러오고, 각 영상의 원본 링크를 함께 제공했습니다.
- 2024 영상 설명과 2026 공개 교육과정, 사용자 제공 최종안 참고 계산을 서로 다른 근거 상태로 표시했습니다.

검증:

- npm.cmd test — Vitest 4.1.8, 51개 테스트 파일·579개 테스트 통과
- npm.cmd run build — TypeScript noEmit과 Vite 7.3.5 프로덕션 빌드 통과, 1,778개 모듈 변환
- Chromium — 1294×912·1024×768·390×844에서 고정 지도, 4개 가이드 주소, 뒤로가기·새로고침, 사용자 동의 전 iframe 미로딩, 선택 영상 주소 복원과 공식 영상 재생을 확인

경계와 남은 리스크:

- 2024 공식 영상이 설명한 신청 시기·변경·증명서 표기는 2026 현재 운영 공지로 다시 확인되지 않았습니다.
- 운영 Vercel에는 아직 배포하지 않았습니다.

### Promote the flat campus journey map and mid-year diagnosis

- 랜딩의 주 경험을 학기표 중심 화면에서 `전공 여정 캠퍼스 지도`로 교체하고, 관심 탐색과 이수 과목 바로 진단이 현재 진단에서 합류하는 흐름을 실제 주소·버튼으로 연결했습니다.
- 캠퍼스 배경을 접힘·구릉·곡선 도로가 없는 평면 2D 안내도로 새로 만들고, 지도 위 SVG도 곡선 대신 수평·직각 경로로 바꿨습니다. 390px 모바일에서는 전체 지도를 축소한 아이콘 지도와 상세 경로 목록을 함께 제공합니다.
- 2·3학년도 목표 트랙 없이 학생 유형과 완료 과목만 입력해도 5개 트랙 진행도를 비교할 수 있게 했습니다. 목표 트랙과 학기 플래너는 결과 이후 선택 사항이며, 나중에 목표를 정해도 과목 검토 시각은 보존합니다.
- 목표 없는 진행도 비교 화면을 `트랙 탐색`이 아니라 `진단 결과`로 표시하고, 관심 설문 완료와 직접 선택한 목표 트랙 상태를 분리해 실제 행동 이력과 지도 상태가 일치하게 했습니다.
- 상단에 학과 홈페이지와 학과 YouTube 공식 링크를 복원하고, 사진을 사용하지 않는 캠퍼스 출처 카드는 화면에서 제거했습니다. 생성 지도는 공식 캠퍼스 지리가 아닌 장식용 개념 이미지입니다.

검증:

- npm.cmd test — Vitest 4.1.8, 50개 테스트 파일·568개 테스트 통과
- npm.cmd run build — TypeScript noEmit과 Vite 7.3.5 프로덕션 빌드 통과, 1,775개 모듈 변환
- git diff --check — 공백 오류 0건
- Chromium — 1440×1100과 390×844에서 평면 지도, 7개 목적지, CTA 주소 이동, 확대·축소·현재 위치, 가로 overflow를 확인

경계와 남은 리스크:

- 평면 지도는 학습 흐름을 설명하는 생성 개념 이미지이며 실제 단국대학교 건물 위치나 공식 캠퍼스 지도를 재현하지 않습니다.
- 2·3학년 진단과 플래너 결과는 제공된 2026 최종계획 기준의 참고 계산입니다. 적용 학번·신청 시기·최종 인정은 학과 확인이 필요합니다.
- push, merge, Vercel 배포, 외부 제출은 수행하지 않았습니다.

## 2026-08-30

### Resolve the planner final-review findings

- 랜딩의 두 진입 CTA가 저장된 프로필·과목 검토 시각·학기 계획을 지우지 않고 선택한 목표만 `profileDraft`에 보관하도록 수정했습니다.
- 결과 현재 화면의 다음 CTA를 실제 이동 대상과 맞는 `다음 수강 후보 확인`으로 바꾸고, 결과 주소는 `current → next → confirm` 순서를 유지했습니다.
- 랜딩 H1의 초기·색인·뒤로가기·새로고침 초점, 안내 모달 제목에서 Shift+Tab 역방향 감금, 저장된 계획 보존을 회귀 테스트로 고정했습니다.
- Compass Path를 아이콘·상태 텍스트·색을 함께 쓰는 연속 종이 경로로 재구성하고, 모바일에서는 가로 넘침 없는 세로 경로로 전환했습니다.
- 로컬 설치 후보와 시스템 fallback만 사용하는 display/body 글꼴 토큰을 추가하고, 1440px 랜딩 H1을 66.24px로 조정했습니다. 원격 글꼴 요청·설치·새 의존성은 추가하지 않았습니다.
- 1440×900 설문에서 `다음` 버튼 하단을 901.09px에서 736.48px로 올려 첫 화면에 완전히 노출했고, 모바일 skip focus와 하단 내비게이션 사이 간격도 다시 확보했습니다.
- 공통 리본·글꼴 영향을 반영해 합성 비개인 승인 캡처 15개를 모두 재생성하고 원본/추적 복사 SHA-256 일치를 확인했습니다.
- 최종 런타임 소스는 `4c110a2b93f86baa09fd92368b5458d2d0aa232e`입니다. 이 항목을 포함하는 후속 커밋은 문서만 변경하며 자기 자신의 해시는 문서 안에 기록하지 않습니다.

검증:

- focused regression — 9개 테스트 파일·89개 테스트 통과
- pnpm.cmd test — Vitest 4.1.8, 47개 테스트 파일·550개 테스트 통과
- pnpm.cmd build — TypeScript noEmit과 Vite 7.3.5 프로덕션 빌드 통과, 1,774개 모듈 변환
- git diff --check — 공백 오류 0건
- Chromium 151 — 저장 계획 CTA 2경로, 랜딩 복귀 초점, 모달 Shift+Tab, 1440/390/320 리본·타이포·설문·overflow·console 확인

경계와 남은 리스크:

- PDF beta는 synthetic-course-history.pdf에서 0 matched / 1 unmatched이며 직접 선택만 완전한 기본 경로입니다.
- 공식 캠퍼스 사진 재사용 허가는 확인되지 않아 앱에 삽입하지 않았고, 개설 이력 공개 재검증은 blocked-by-public-access입니다.
- Impeccable detector was run exactly once in Task 11 and failed before JSON because the installed detect-url.mjs was missing. Do not rerun it or alter/install the skill. Preserve this limitation verbatim.
- push, merge, deploy, 외부 제출은 수행하지 않았습니다.

### Validate the planner visual redesign release gate

- 2026-08-31 KST에 캠퍼스 안내책자 × 학업 플래너 시각 체계를 실제 프로덕션 미리보기로 검증했습니다.
- fresh 관심 설문 10문항 경로, fresh 직접 진단 경로, PDF beta 검수·취소, 결과·독립 추천·학기 계획, 자료 읽기 4개 주소를 완주했습니다.
- 1440×900, 1024×768, 768×1024, 390×844, 320×800에서 보이는 H1/main 각 1개, 가로 overflow·수평 잘림·44px 미만 조작 요소 0건을 확인했습니다.
- 키보드 전용 직접 진단, 3px skip-link focus, reduced motion, schedule/checks history 복원, 합성 localStorage 실패 alert, 모바일 하단 내비게이션 비겹침을 확인했습니다.
- 결과 2쪽과 계획 3쪽을 print media PDF로 저장하고 전 페이지 PNG를 육안 검수했습니다.
- 합성 비개인 QA 상태의 승인 캡처 15개를 docs/assets/2026-08-30-planner-redesign/에 추가하고 원본과 SHA-256 일치를 확인했습니다.

검증:

- pnpm.cmd test — Vitest 4.1.8, 46개 테스트 파일·544개 테스트 통과
- pnpm.cmd build — TypeScript noEmit과 Vite 7.3.5 프로덕션 빌드 통과, 1,774개 모듈 변환
- git diff --check — 공백 오류 0건
- 로컬 Chromium 151 — 5개 핵심 흐름, 5개 뷰포트, 키보드·히스토리·인쇄·개인정보·이미지 fallback 확인

경계와 남은 리스크:

- 이수 과목 직접 선택은 완전한 기본 경로입니다. PDF beta는 synthetic-course-history.pdf에서 0 matched / 1 unmatched라 release-ready로 판정하지 않았습니다.
- localStorage에는 앱 상태 키 2개만 남고 PDF 원래 파일명·원문 오탈자·개인 식별 sentinel은 없었습니다. 네트워크는 localhost 정적 GET만 있었고 업로드·POST는 없었습니다.
- 생성 이미지는 공식 캠퍼스 사진·로고·인장·근거 자료가 아닙니다. 공식 캠퍼스 사진은 재사용 허가 미확인으로 삽입하지 않았습니다.
- Impeccable detector was run exactly once in Task 11 and failed before JSON because the installed detect-url.mjs was missing. Do not rerun it or alter/install the skill. Preserve this limitation verbatim.
- 이번 작업은 push, merge, deploy, 외부 제출을 수행하지 않았습니다. 운영 Vercel에 반영됐다고 주장하지 않습니다.

### Validate independent recommendations and graduation planning

- 관심 적합도, 현재 완료 과목 접근성, 졸업 전 계획 가능성을 하나의 종합 순위로 합치지 않고 독립 축으로 검증했습니다.
- 정규학기 계획 가능, 학기당 수강량 조정, 추가 학기 필요 가능성, 공식 확인 필요 결과를 실제 브라우저에서 각각 재현했습니다.
- 모바일 계획 화면의 단계 전환·뒤로가기·앞으로가기에서 새 H1 포커스와 상단 스크롤을 복원하고, 계획 화면을 벗어나면 브라우저의 기본 스크롤 정책으로 돌아오는지 확인했습니다.
- 현재 HEAD에서 데스크톱 1440×900, 모바일 390×844 캡처 6개를 다시 만들고 육안 검수 및 원본/문서 자산 SHA-256 동일성을 확인했습니다.

검증:

- `pnpm.cmd run test` — 22개 테스트 파일, 278개 테스트 통과
- `pnpm.cmd run build` — TypeScript 검사 및 Vite 프로덕션 빌드 통과, 1,722개 모듈 변환
- 로컬 Chromium — 직접 진단, 설문→트랙→프로필→과목→결과, 일치·불일치 추천 축, 4개 계획 상태, 저장·back·forward·reload, 키보드 전용 핵심 흐름 통과
- 모든 승인 캡처에서 콘솔 오류·경고 0건, 가로 넘침 0px

근거와 다음 계획 경계:

- 2026-1·2 개설값은 확보 당시의 과거 스냅샷이며, 현재 공개 원문의 재검증은 차단되어 있습니다. 미래 반복 개설과 공식 졸업·트랙 이수를 보장하지 않습니다.
- 이수 과목은 직접 선택이 기본입니다. 기존 `PDF 저장/인쇄`는 브라우저 결과 출력이며 성적표 PDF 불러오기가 아닙니다.
- 브라우저 내부 PDF 불러오기 beta, 저장된 진단 변화 비교, 설명 이미지 3개 슬롯, 공식 캠퍼스 사진의 라이선스 확인과 전체 E2E QA는 다음 계획으로 남겼습니다.

### Record track-diagnosis foundation validation

- 학생 소속·이수 경로 분리, 규칙 근거 상태, 필수·트랙·전체학점 진행도, 저장 v2 마이그레이션, 주소·새로고침 복원을 기반 기능으로 기록했습니다.
- 공식 공개 자료는 규칙 확인의 출발점이며, 화면 계산은 제공된 최종계획을 구조화한 기준으로 수행합니다.
- 개인별 적용 학번, 필수 변형, 기타 인정학점, 경제학 트랙형 복수전공 최소학점은 학과 검토 전까지 참고 결과로만 취급합니다.
- 데스크톱·모바일 핵심 화면 캡처 5개와 SHA-256을 검증 보고서에 보존했습니다. 원본 QA 파일은 `output/`에 남겨 두고, 이후 임시 출력은 Git에서 제외합니다.

검증:

- `pnpm.cmd run test` — 9개 테스트 파일, 82개 테스트 통과
- `pnpm.cmd run build` — TypeScript 검사 및 Vite 프로덕션 빌드 통과
- `git diff --check` — 공백 오류 없음

검증 한계 및 다음 계획:

- 데스크톱·모바일 핵심 경로, 뒤로가기·앞으로가기·새로고침, 콘솔 오류·경고, 가로 넘침은 브라우저에서 확인했습니다.
- 키보드만으로 프로필→과목→결과를 완주하는 흐름과 강제 저장 실패 시 알림 표시는 브라우저에서 아직 확인하지 않았습니다. 저장 실패 처리 자체는 자동 테스트로 확인합니다.
- 사전 존재 기능인 결과 화면의 브라우저 인쇄 대화상자 PDF 저장은 이번 기반 릴리스 게이트의 신규 검증 범위가 아닙니다. PDF 과목 불러오기·자동 채움과 이미지 생성은 다음 계획 범위이며 이번 기반 검증에서 구현하거나 판정하지 않았습니다.

## 2026-08-25

### Complete the product-led diagnosis journey

- 랜딩페이지를 트랙제 의미, 선택 이유, 서비스 이용 흐름 중심으로 줄여 긴 스크롤과 반복 설명을 정리했습니다.
- 자가진단 표를 학년·학기별 목록과 모듈별 목록 전환 구조로 바꾸고 과목 검색을 추가했습니다.
- 결과에 `맞춤 트랙 추천` 탭을 추가해 가장 가까운 트랙, 추가 후보, 공통 도움 과목을 설명합니다.
- 학기 계획에서 추천 과목을 `다음 학기 · 다다음 학기 · 나중에`로 배치하고 브라우저에 저장할 수 있게 했습니다.
- 실제 이수 과목과 계획 과목을 분리해, 계획은 사용자가 선택한 경우에만 추천 비교에 반영합니다.
- 토스 TDS의 회색 단계와 녹색 상태색, 48px 조작 영역, 당근 사례의 행동 발견 가능성, AI 템플릿의 반복 카드·그라데이션 회피 기준을 적용했습니다.

검증:

- `npm test` 기준 23개 테스트 통과
- `npm run build` 기준 TypeScript 검사 및 Vite 프로덕션 빌드 통과
- 인앱 브라우저 1440×1024와 390×844에서 랜딩, 트랙 선택, 과목 체크, 보기 전환, 맞춤 추천, 계획 저장 흐름 확인
- 모바일 `scrollWidth === viewport width`와 브라우저 오류 0건 확인

남은 리스크:

- 과목 개설 시기와 실제 트랙 인정은 학과 최신 안내가 최종 기준입니다.
- 추천은 입력 과목과 구조화한 2026 교육과정만을 기준으로 하며 진로나 개인 선호를 확정하지 않습니다.

### Add service-first landing page

- 기존 진단 기능 앞에 서비스 목적, 트랙제 구조, 이용 흐름을 설명하는 반응형 랜딩페이지를 추가했습니다.
- `과목 → 모듈 → 트랙` 관계, 다섯 트랙 비교, 3단계 진단 흐름, 결과 예시를 한 페이지에서 확인할 수 있도록 구성했습니다.
- 결과 예시와 트랙 선택 탭은 실제 선택 상태가 바뀌도록 구현했습니다.
- 진단 화면의 학과 로고를 누르면 랜딩페이지로 돌아올 수 있게 연결했습니다.
- 화면 전반을 흰색·쿨그레이 배경, 단국대 블루, 포레스트 그린 기반의 절제된 디자인으로 정리했습니다.
- README의 첫 화면 이미지도 새 랜딩페이지로 교체했습니다.

공식 근거:

- [2026학년도 공식 공개 PDF 교육과정 원문 — 규칙 검증의 출발점이며 현재 계산 입력 자체는 아님](https://www.dankook.ac.kr/documents/d/kor/2026-1-_-260119-pdf?download=true)
- [식품자원경제학과 공식 트랙제 안내 영상](https://www.youtube.com/watch?v=osc9yOuq0IU)
- [식품자원경제학과 홈페이지](https://cms.dankook.ac.kr/web/ere)

검증:

- `pnpm run test` 기준 20개 테스트 통과
- `pnpm run build` 기준 TypeScript 검사 및 Vite 프로덕션 빌드 통과
- 인앱 브라우저에서 데스크톱 1440×1024, 모바일 390×844 화면과 고정 헤더, 탭, 진단 진입·랜딩 복귀 흐름 확인
- 브라우저 오류·경고 로그 없음

남은 리스크:

- 트랙 인정, 변경, 중복 모듈 인정 기준은 학과의 최신 공지와 상담 결과가 최종 기준입니다.
- 구현본은 아직 원격 저장소의 본 작업 브랜치나 운영 Vercel에 배포하지 않았습니다.

### Redesign the internal diagnosis flow

- 내부 사이드 메뉴 7개를 `자가진단 → 결과 → 학기 계획` 3단계 상단 내비게이션으로 축소했습니다.
- 트랙 선택을 완료하면 입력 영역을 접고 과목 선택에 바로 집중할 수 있게 했습니다.
- 과목표는 화면 안에서 스크롤하도록 제한하고, 모바일에서는 학기별 세로 목록으로 전환했습니다.
- 학기 미정 과목, 5개 트랙 상세 순위, 추가 안내 영상, 전체 교육과정표는 필요할 때 펼쳐보는 구조로 변경했습니다.
- 결과 화면을 `한눈에 보기 · 부족 모듈 · 필수 과목` 탭으로 나눴습니다.
- 트랙 추천과 실험실을 `학기 계획` 아래 `트랙 추천 · 학기별 계획` 탭으로 통합했습니다.
- 기존 하늘색·연두색 그라데이션과 반복 카드 표현을 제거하고 랜딩과 같은 흰색·쿨그레이·네이비·포레스트 그린 체계를 적용했습니다.
- 토스 TDS, 토스 제품 원칙, 당근 SEED, WCAG 자료를 정리한 내부 UX 리서치 문서를 추가했습니다.

검증:

- `pnpm run test` 기준 20개 테스트 통과
- `pnpm run build` 기준 TypeScript 검사 및 Vite 프로덕션 빌드 통과
- 데스크톱 1440×1024와 모바일 390×844에서 주요 단계, 탭, 트랙 선택 접기·수정, 과목 목록을 확인했습니다.

## 2026-06-11

### Add production deployment URL to repository docs

- README 상단에 Vercel 프로덕션 URL을 추가했습니다.
- `package.json`에 `homepage`와 GitHub 저장소 URL을 추가해 저장소 메타 정보로도 배포 주소를 남겼습니다.

배포 URL:

- https://dku-track-diagnosis.vercel.app

검증:

- 배포 URL 응답 확인

### Fix Vercel install configuration

- Vercel 배포 중 `pnpm install --frozen-lockfile` 단계가 실패해 lockfile을 `package.json` 기준으로 재생성했습니다.
- Vercel 원격 설치 단계의 pnpm 실행 문제가 반복되어 Vercel 배포 설정을 `npm install` / `npm run build` 기준으로 전환했습니다.
- 로컬 Vercel 연결 폴더가 저장소에 올라가지 않도록 `.vercel`을 `.gitignore`에 추가했습니다.

검증:

- `pnpm run test`
- `pnpm run build`

### Repository documentation cleanup

- README에 서비스 첫 화면 스크린샷과 사이트 목적, 주요 기능, 실행/검증 방법을 정리했습니다.
- 협업을 위한 `CONTRIBUTING.md`와 PR 템플릿을 추가했습니다.
- GitHub 저장소 루트에 잘못 포함된 `-` 하위 저장소 항목을 제거했습니다.

검증:

- 문서 변경 중심입니다.
- 앱 스크린샷은 로컬 실행 화면 기준으로 생성했습니다.

### Add lab feasibility and deployment setup

커밋: [`ee2160e`](https://github.com/Suya020504/dku-track-diagnosis/commit/ee2160e3ee5a526d7108da178e730f8c47be5971)

- 실험실 탭에 현재 학년/학기 입력 기능을 추가했습니다.
- 남은 정규학기 기준으로 트랙 달성 가능성을 `정규학기 안에 가능`, `초과학기 진행시 가능`, `장기 계획 필요` 등으로 구분했습니다.
- 복수 트랙 선택 시 겹치는 부족 모듈과 공통 수강 추천 과목을 표시했습니다.
- Vercel 배포 설정 파일을 추가했습니다.
- README에 기본 실행, 검증, 배포 설정을 정리했습니다.

검증:

- `pnpm run test`
- `pnpm run build`
- 브라우저에서 실험실 탭의 학기 입력, 가능성 분류, 겹치는 모듈 추천 영역을 확인했습니다.

### Initial commit

커밋: [`5fbfaf5`](https://github.com/Suya020504/dku-track-diagnosis/commit/5fbfaf564f94d6c376f006f158999d156a956d67)

- Vite + React + TypeScript 기반 정적 웹앱을 구성했습니다.
- 2026학년도 식품자원경제학과 트랙/모듈/과목 데이터를 코드와 분리해 구조화했습니다.
- 트랙제 설명, 트랙/모듈, 자가진단, 결과, 도구/정보, 문의사항 화면을 구현했습니다.
- 트랙별 모듈 충족 여부, 부족 학점, 필수 과목 누락, 추천 과목 계산 로직을 구현했습니다.
- 진단 로직 테스트를 추가했습니다.
- 단국대학교 및 식품자원경제학과 톤을 반영한 기본 디자인과 이미지 자산을 추가했습니다.

검증:

- 주요 진단 계산 케이스를 테스트로 확인했습니다.
- 로컬 브라우저에서 주요 탭과 반응형 레이아웃을 확인했습니다.
